# Hubspot Duplicate Deals Delete

Simple script that searches in Hubspot for duplicated deals and deletes one of them. 

Duplicated deals are defined as 
- Same *name*
- Same *amount*
- Only in *default* pipeline, stage *deal won* and *deal lost*